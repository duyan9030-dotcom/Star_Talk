<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.startalk.startalk.conversation.mapper.SessionMapper">

    <resultMap id="BaseResultMap" type="com.startalk.startalk.conversation.entity.ConversationSession">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="scene_id" property="sceneId"/>
        <result column="scene_name" property="sceneName"/>
        <result column="status" property="status"/>
        <result column="message_count" property="messageCount"/>
        <result column="start_time" property="startTime"/>
        <result column="end_time" property="endTime"/>
        <result column="duration" property="duration"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT * FROM conversation_session
        WHERE user_id = #{userId} AND deleted = 0
        ORDER BY create_time DESC
    </select>

    <select id="selectActiveByUserId" resultMap="BaseResultMap">
        SELECT * FROM conversation_session
        WHERE user_id = #{userId} AND status = 1 AND deleted = 0
        LIMIT 1
    </select>

</mapper>