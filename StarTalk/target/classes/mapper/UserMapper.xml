<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.startalk.startalk.mapper.UserMapper">

    <resultMap id="BaseResultMap" type="com.startalk.startalk.entity.User">
        <id column="id" property="id"/>
        <result column="openid" property="openid"/>
        <result column="unionid" property="unionid"/>
        <result column="phone" property="phone"/>
        <result column="nickname" property="nickname"/>
        <result column="avatar" property="avatar"/>
        <result column="age" property="age"/>
        <result column="grade" property="grade"/>
        <result column="role" property="role"/>
        <result column="status" property="status"/>
        <result column="password" property="password"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <select id="selectByOpenid" resultMap="BaseResultMap">
        SELECT * FROM sys_user WHERE openid = #{openid} AND deleted = 0
    </select>

    <select id="selectByPhone" resultMap="BaseResultMap">
        SELECT * FROM sys_user WHERE phone = #{phone} AND deleted = 0
    </select>

    <select id="selectByUnionid" resultMap="BaseResultMap">
        SELECT * FROM sys_user WHERE unionid = #{unionid} AND deleted = 0
    </select>

</mapper>